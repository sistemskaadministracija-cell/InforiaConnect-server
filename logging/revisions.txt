InforiaConnect Server Revision Log
==================================

Timestamp: 2026-06-06T00:54:07+02:00
Title: Add reloadable InforiaConnect device ID blocklist

Changed files:
  - .github/workflows/inforia-server-build.yml
  - AGENTS.md
  - deploy/docker-compose.yml
  - logging/revisions.txt
  - scripts/block-id.sh
  - scripts/list-blocked-ids.sh
  - scripts/unblock-id.sh
  - src/blocked_ids.rs
  - src/lib.rs
  - src/main.rs
  - src/rendezvous_server.rs

Changes:
  - Added a file-backed numerical ID blocklist that reloads automatically.
  - Blocked devices are denied registration, shown as offline, and cannot be
    targeted through punch-hole or relay setup requests.
  - Added a dedicated server response value for the custom InforiaConnect
    client to display an administrator-blocked status.
  - Added block, unblock, and list helper scripts for the persistent VM data
    directory without embedding production IDs in Git.
  - Added a deployment compose file that preserves host networking, restart
    policy, server address, key mode, and the existing persistent data mount.
  - Added a focused GitHub Actions workflow that builds static hbbs/hbbr
    binaries and publishes the custom image to GitHub Container Registry.

Reasons:
  - Allow administrators to revoke a known InforiaConnect device ID without
    deleting client configuration or changing router and firewall settings.
  - Keep blocklist operations auditable, reversible, and independent from
    client or server image rebuilds.

Validation:
  - Confirmed Git diff whitespace checks pass.
  - Confirmed the helper scripts pass shell syntax validation.
  - Local Rust compilation is unavailable because this Windows environment
    lacks the MSVC linker; the Linux GitHub Actions build is authoritative.
  - Intended Actions run name: ID blocklist server image.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T03:03:15+02:00
Title: Harden production server containers

Changed files:
  - deploy/docker-compose.yml
  - logging/revisions.txt

Changes:
  - Made the hbbs and hbbr image filesystems read-only.
  - Dropped all Linux capabilities.
  - Enabled no-new-privileges for both containers.

Reasons:
  - Reduce the impact of a process compromise while retaining write access
    only through the persistent `/root` data mount.

Validation:
  - Recreated both production containers with automatic rollback protection.
  - Both containers remained running and all required listeners returned.
  - Docker inspect confirmed read-only root filesystems, capability drop ALL,
    and no-new-privileges.
  - No network, firewall, DNS, routing, or SSH setting changed.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T02:21:15+02:00
Title: Deploy and pin the validated custom server image

Changed files:
  - deploy/docker-compose.yml
  - logging/revisions.txt

Changes:
  - Pinned both server services to the exact image digest that passed CI.
  - Deployed the custom hbbs and hbbr containers on the Linux VM.
  - Preserved host networking, restart policy, persistent data, server key,
    database, relay address, and all application ports.
  - Added an empty persistent blocked_ids.txt file.
  - Hardened local permissions on the server private key and database.

Reasons:
  - Activate the ID blocklist server reproducibly without changing network
    configuration or risking key/database replacement.

Validation:
  - Created and checksummed a consistent pre-deployment backup.
  - Verified both digest-pinned containers are running after restart.
  - Verified TCP 21115-21119 and UDP 21116 listeners.
  - Verified the public-key checksum is unchanged.
  - Verified hbbs loaded zero blocked IDs from the persistent file.
  - Verified private key and database mode 0600 and data directory mode 0750.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T01:53:27+02:00
Title: Preserve executable permissions in server image

Changed files:
  - docker-classic/Dockerfile
  - logging/BuildError_log.txt
  - logging/revisions.txt

Changes:
  - Applied mode 0755 to hbbs and hbbr with Docker COPY --chmod.
  - Logged the confirmed permission-denied smoke-test failure.

Reasons:
  - Artifact transport removed executable mode before the scratch image was
    built, so Docker could locate but not execute the binaries.

Validation:
  - The server code, tests, static build, image build, and publish all passed.
  - GitHub Actions run 27046280606 passed both published-image smoke tests.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T01:16:40+02:00
Title: Use absolute server binary paths in containers

Changed files:
  - .github/workflows/inforia-server-build.yml
  - deploy/docker-compose.yml
  - logging/BuildError_log.txt
  - logging/revisions.txt

Changes:
  - Updated smoke-test entrypoints to `/usr/bin/hbbs` and `/usr/bin/hbbr`.
  - Updated production Compose commands to the same absolute paths.
  - Preserved the previous failed attempt and recorded the repeated failure.

Reasons:
  - Docker does not use the image PATH when resolving an explicit entrypoint
    name in this scratch image.

Validation:
  - Confirmed both binaries are copied to `/usr/bin` by the image Dockerfile.
  - Confirmed the server code and blocklist tests passed in both prior runs.
  - Intended Actions run name: Use absolute server container paths.

-------------------------------------------------------------------------------

Timestamp: 2026-06-06T01:06:07+02:00
Title: Make server binaries executable by container command name

Changed files:
  - docker-classic/Dockerfile
  - logging/BuildError_log.txt
  - logging/revisions.txt

Changes:
  - Added `/usr/bin` to the scratch image PATH.
  - Logged the confirmed image smoke-test failure and corrective action.

Reasons:
  - The image contained `/usr/bin/hbbs` and `/usr/bin/hbbr`, but Docker could
    not resolve the command names used by the smoke test and deployment file.

Validation:
  - The previous workflow successfully compiled, tested, and published the
    image; only command resolution failed.
  - Confirmed the Dockerfile copy destinations match the new PATH.
  - Intended Actions run name: Fix server image binary PATH.

-------------------------------------------------------------------------------
